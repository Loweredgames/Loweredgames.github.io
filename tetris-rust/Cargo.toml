[package]
name = "tetris-rust"
version = "0.1.0"
edition = "2021"
description = "Piccolo gioco di Tetris compilabile in Rust e WebAssembly."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.128"
